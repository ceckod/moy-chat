# CD-B Records — Control Dashboard

Единен HTML файл (SPA), който управлява целия музикален процес в 4 стъпки.
Работи изцяло в браузъра — без Python бекенд, без сървър. Всички ключове
и данни се пазят локално на устройството (localStorage).

## Файлове
- `index.html` — цялата структура и стилове
- `app.js` — цялата логика (API извиквания, чеклист, навигация)
- `manifest.json` — за да можеш да добавиш иконата на телефона си
- `assets/icon-192.png`, `assets/icon-512.png` — **трябва да ги добавиш сам**
  (лого на CD-B Records в квадратен формат, PNG, прозрачен фон)

## Как да го качиш в GitHub Pages

1. Създай нов repo в GitHub (може да е публичен — няма тайни ключове в кода).
2. Качи `index.html`, `app.js`, `manifest.json` и папка `assets/` с иконите.
3. Отиди в **Settings → Pages** на repo-то → избери branch `main` → Save.
4. След 1-2 минути сайтът ти ще е достъпен на:
   `https://<твоя-username>.github.io/<repo-name>/`

## Как да го сложиш като икона на телефона

**Android (Chrome):** Отвори линка → меню (⋮) → "Add to Home screen".
**iPhone (Safari):** Отвори линка → бутон Share → "Add to Home Screen".

## Първо стартиране — API ключове

Отвори приложението → бутон **⚙️ API Ключове / Настройки** горе-долу вляво →
въведи:
- **Anthropic (Claude) API Key** — от console.anthropic.com
- **Google Gemini API Key** — от aistudio.google.com
- **Google Client ID (OAuth)** — от Google Cloud Console (за YouTube upload).
  Трябва да разрешиш "YouTube Data API v3" в проекта си и да добавиш
  твоя GitHub Pages домейн в "Authorized JavaScript origins".

Всичко се пази само в браузъра ти — нищо не се качва в GitHub repo-то.

## Известни ограничения / TODO

- **Стъпка 2 (Визуализатор):** Мястото е подготвено в `index.html`
  (секция `panel-2`) и в `app.js` (обект `Step2`). Твоят съществуващ
  визуализатор (видео1 + видео2 + лого) трябва да се вгради тук —
  прати кода му (без самите видео файлове) за интеграция.
- **Стъпка 3 (DistroKid):** Реализирано като *auto-fill асистент* —
  генерира текстовете, но не автоматизира самия DistroKid сайт
  (браузърът не може да управлява друг сайт по security причини).
- **Обложка (Imagen):** Ако директното извикване от браузъра върне
  CORS грешка, попълни Proxy URL в Настройки (виж по-долу).
- **Google/YouTube Trends за Стъпка 1:** Бутонът "Предложение за
  песен" сега прави реално сканиране чрез Gemini + Google Search
  grounding (не само общи познания). За най-точни числа може
  допълнително да се добави пряка YouTube Data API `search.list`
  заявка.

## Нови функции

- **Авто trend-scan:** остави полето за ниши празно и натисни
  "🔍 Предложение за песен" — Gemini търси в реално време какво
  расте в момента и връща топ 5 с индикатор 🟢>75/🟡>50/⚪.
- **📊 YouTube Outlier анализ:** автоматично след избора на ниша —
  търси видеа по нишата и сверява views/subscribers; малки канали
  с непропорционално много гледания = сигнал за търсене без силна
  конкуренция (VidIQ-стил). Изисква YouTube Data API Key.
- **🔎 Свързани търсения:** реални autocomplete предложения (какво
  реално дописва/търси аудиторията) — musicalSEO-подобен ефект.
  Изисква Proxy URL в Настройки (endpoint-ът няма CORS хедъри).
- **📀 Album Sprint:** генерира 10-30 различни заглавия+hook идеи
  наведнъж в избраната ниша — опашка от идеи вместо 1 по 1.
- **Gemini Validator:** авто-анализ (3 изречения) след всяка стъпка
  (trend scan, концепция, текст, FX, обложка, album sprint) — трупа
  се в лог, видим в Стъпка 1.
- **Proxy URL (Настройки):** по избор — ако директните заявки към
  Gemini/Imagen/suggest гърмят с CORS, сложи URL на малък
  reverse-proxy (напр. Cloudflare Worker) който приема
  `?target=ORIGINAL_URL` и препраща заявката 1:1. Празно поле =
  директни заявки (стандартно).
- **🧪 Тествай ключовете (Настройки):** прави по едно евтино
  тестово извикване към Claude/Gemini/YouTube Data API и показва
  ✅/❌ за всеки.
- **Export/Import проект (Настройки):** проектът живее само в
  localStorage на това устройство — Export преди да чистиш кеша
  или сменяш телефон/браузър; Import връща всичко обратно.
- **🆕 Нов проект (Настройки):** изчиства текущата песен (заглавие,
  текст, лог), без да пипа запазените API ключове.

## Идеи за следващо (не са реализирани, кажи ако искаш някоя):

- Многопроектна история (списък с предишни песни, не само 1 активна).
- Offline кеширане (service worker) — работи и без връзка, освен
  самите AI извиквания.
- Автоматична проверка за YouTube upload лимит/quota преди опит.
- Проверка на свободен домейн за бранд името на изпълнителя.
- Voice prompting през вградения браузърен Web Speech API.
